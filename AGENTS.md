# 项目执行约束

- 本仓库以及所有构建、下载、缓存和实验输出必须位于本目录内；不得向父目录或系统目录写入项目文件。
- `references/` 与 `Datasets/` 仅作为仓库外的只读输入。受限许可证代码不得提交到本仓库。
- 自有 C/C++/CUDA 代码采用 C++20，公开符号使用英文；关键算法、边界条件和安全回退使用中文注释。
- 任何边删除必须经过同一不可变快照上的 CPU 精确验证；GPU 和浮点 LP 结果只能生成候选，不能直接修改图。
- 不支持的距离类型、溢出风险、证书不完整或求解状态异常都必须安全失败，保留边。
- 临时内容分别放入 `.tmp/`、`.deps/`、`.venv/`、`build/` 或 `artifacts/`，这些目录不得提交。
- 提交前至少运行 CPU 单元测试；涉及 CUDA 时还要运行 GPU 差分测试和 `compute-sanitizer`。
